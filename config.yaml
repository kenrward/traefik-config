http:
  routers:
    traefik-dashboard:
      rule: "Host(`traefik.kewar.org`)" # Replace with your domain
      entryPoints:
        - websecure
      service: api@internal
      tls: {}

  middlewares:
    https-redirect:
      redirectScheme:
        scheme: https
        permanent: true